# 🎬 Anime Finder (글로벌 애니 탐색) 개발자 가이드

안녕하세요! 이 프로젝트는 전 세계의 다양한 애니메이션이 어느 OTT(넷플릭스, 디즈니+ 등)에서 스트리밍 중인지 알려주고, **Google 번역**을 통해 전 세계 언어로 감상할 수 있게 돕는 글로벌 서비스입니다.

---

## 🏗 시스템 구성 및 기술 스택

1.  **다국어 (i18n)**: `next-intl`을 써서 전 세계 사용자가 본인 언어로 화면을 볼 수 있게 했습니다. 
2.  **번역 엔진**: Google Cloud Translate API를 연동해서 작품 줄거리를 실시간으로 번역해 줍니다.
3.  **UI/UX**: `Swiper`와 `Framer Motion`을 활용해서 마치 넷플릭스 앱을 쓰는 듯한 고급스러운 슬라이드와 애니메이션을 구현했습니다.

---

## 🛠 주요 개발 명령어

- `npm run dev`: 최신 **Next.js 16**과 **Turbopack**의 속도를 느끼며 개발할 때 실행하세요.
- `npm run build`: 실제 서비스 배포를 위해 빌드할 때 사용합니다.

---

## 💡 개발자를 위한 팁

### 언어 설정을 바꾸고 싶다면?
- `src/middleware.ts`를 확인하세요. 여기서 기본 언어(ko, en 등)와 주소창에 `/ko`가 붙는 방식을 결정합니다.

### 이미지 로딩 최적화
- 애니메이션 포스터는 고화질이 많아서 페이지가 느려질 수 있습니다. `next/image` 컴포넌트의 설정을 통해 이미지 최적화가 잘 작동하는지 확인하세요.

---

## 🚨 문제 해결 (Troubleshooting)

**Q: 번역이 안 되고 원문만 나와요.**
- A: `.env` 파일의 `GOOGLE_TRANSLATE_API_KEY`가 올바른지, 혹은 Google Cloud 설정에서 번역 API 한도를 초과하지 않았는지 확인해야 합니다.

**Q: 화면 레이아웃이 깨져요.**
- A: 이 프로젝트는 **Tailwind CSS 4**를 사용합니다. 구버전에서 지원하지 않는 새로운 문법을 썼을 수 있으니 스타일 파일(`globals.css`)을 확인하세요.

---

## 📁 주요 구성 요소
- `messages/`: 다국어 UI 문규(json)들이 저장된 곳.
- `src/app/`: 국가별 경로 처리 및 화면 레이아웃 소스.
- `src/components/`: 고급 애니메이션이 적용된 카드 및 슬라이더 컴포넌트.
